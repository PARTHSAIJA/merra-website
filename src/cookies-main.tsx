import React from 'react'
import ReactDOM from 'react-dom/client'
import CookiePolicyApp from './CookiePolicyApp'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CookiePolicyApp />
  </React.StrictMode>,
)
