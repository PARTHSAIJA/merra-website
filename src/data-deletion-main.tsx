import React from 'react'
import ReactDOM from 'react-dom/client'
import DataDeletionApp from './DataDeletionApp'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DataDeletionApp />
  </React.StrictMode>,
)
